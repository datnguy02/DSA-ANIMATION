import { createContext } from "react";

export const HeadRefContext = createContext(null);
export const HeadLineContext = createContext(null);
export const TailLineXContext = createContext(null);
export const TailRefContext = createContext(null);
export const TailLineYContext = createContext(null);
export const HeadRefTextContext = createContext(null);
export const TailRefTextContext = createContext(null);

